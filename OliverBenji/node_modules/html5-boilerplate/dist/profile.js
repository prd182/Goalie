window.onload =function() {
    welcomeMessage();
}

function welcomeMessage() {
    let currUser = localStorage.getItem('user'.valueOf);
    let welcomeBox = document.getElementsByClassName('welcome');
    
    let welcomeMessage = `
    <h2>Welcome ${currUser}</h2>
    `;
    welcomeBox.appendTo(welcomeMessage);
}

function createGame() {
    var newGame = document.getElementById(gameform):

    var newGameForm = 
}