define(['controllers/login-controller'],function (loginController) {
    var internals = {};
    var externals = {};
     
    externals.userId = '';
  
    externals.AuthUser = async (userId) => {
        let fields = await fetch(`http://192.168.10.172:8080/goalie/api/users/${userId}`);
        
        let result = await fields.json();
        console.log(result)

            // Handle the authentication result
            if (result) {
                // Authentication successful, redirect or perform other actions
                window.location.hash = "#field";
            } else {
                // Authentication failed, handle accordingly
                console.log("Authentication failed");
            }
    };


    return externals;
})