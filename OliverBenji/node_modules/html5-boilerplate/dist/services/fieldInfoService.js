define(function () {
    var internals = {};
    var externals = {};

    externals.id = '';

    externals.getFieldInfo = (cb) =>{
        console.log(`info id:${externals.id}`)
        cb(externals.id)
    }

    return externals
});