define(function(){

    var internals = {
        handlers: {},
        elements: {}
    };
    var externals = {};

    externals.render = (id) =>{
        $(".container").empty();
        console.log(`rendering fieldInfoView ${id}`)
    }

    internals.renderReturnButton = () =>{
        
    }

    return externals
})